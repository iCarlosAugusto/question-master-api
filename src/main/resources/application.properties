# Application Configuration
spring.config.import=optional:file:.env
spring.application.name=gamified-question-api
spring.profiles.active=dev

# Server Configuration
server.port=8080
server.servlet.context-path=/

# Database Configuration
# Use environment variables for sensitive data
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?user=postgres.swzkcdgnahprrfzpfaoi&password=LeuY2TDZ6zreGbPp
spring.datasource.username=postgres.swzkcdgnahprrfzpfaoi
spring.datasource.password=LeuY2TDZ6zreGbPp
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Configuration
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=false
spring.flyway.validate-on-migrate=true

# JWT Configuration
# Use environment variable JWT_SECRET or fallback to default (NOT RECOMMENDED FOR PRODUCTION)
# If JWT_SECRET is not set, the default value will be used
app.jwt.secret=+zpR/X0Hgds89beVkgtFrnkqx8tzt0pJ/E0EE4emvHInj8XgHI8+AFR14eLzfoGBZ92vUe7TEwox6l8OCT8EMg==
app.jwt.expiration=${JWT_EXPIRATION:86400000}

# Encryption Configuration (AES-256-GCM)
# Generate a 32-byte (256-bit) key and encode it in Base64
# Example: openssl rand -base64 32
# Use environment variable ENCRYPTION_KEY (REQUIRED FOR PRODUCTION)
app.encryption.key=ZTNiNzlhNGY1ZDI5Yzg3YTFiNmYwZDI0ZTkxYTM3YzI=

# Encryption Debug (development only)
# Set to true to enable /api/encryption/key endpoint
# Use environment variable ENCRYPTION_DEBUG
app.encryption.debug=${ENCRYPTION_DEBUG:false}

#Internal
internal.api.key=cGVyc29ucGhvbmU2MjIwMDMhQGF1Z3VzdG9wYXNzd29yZA==

# OpenAPI Configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Logging Configuration
logging.level.com.questionmaster.api=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=UTC
