# Development Profile Configuration

# Database Configuration
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?user=postgres.swzkcdgnahprrfzpfaoi&password=LeuY2TDZ6zreGbPp
spring.datasource.username=postgres.swzkcdgnahprrfzpfaoi
spring.datasource.password=LeuY2TDZ6zreGbPp
spring.datasource.driver-class-name=org.postgresql.Driver

# HikariCP Configuration  
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# Flyway Configuration - Temporarily disabled to fix schema issues
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=false
spring.flyway.clean-on-validation-error=true
spring.flyway.validate-on-migrate=false

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Logging Configuration
logging.level.com.questionmaster.api=DEBUG
logging.level.org.springframework.security=DEBUG

# JWT Configuration (less secure for development)
app.jwt.secret=devSecretKeyForDevelopmentOnlyNotForProduction
app.jwt.expiration=86400000

# CORS Configuration (allow all for development)
cors.allowed-origins=http://localhost:3000,http://localhost:3001
cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true
